import { DebounceResizeDirective } from './debounce-resize.directive';

describe('DebounceResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceResizeDirective();
    expect(directive).toBeTruthy();
  });
});
